These plugins are distributed with the virtualop, but not enabled by default.

To enable them, either use
```
configure_<plugin_name>
```
on the command line or go to "Config | Plugins" in the web interface.
