description "installation script for <%= service_name %>"

param :machine
<%= params['extra_install_command_header'] ? params['extra_install_command_header'] : '' %>

on_machine do |machine, params|

end
